export class Foo {
  public name: string;
  public description: string;
  public id: number;

  constructor(name: string, desc: string, id: number) {
    this.name = name;
    this.description = desc;
    this.id = id;
  }
}
