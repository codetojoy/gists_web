/*
import { Bar} from './bar.model';

describe.each`
  s              | n             | expected
  'abc'   | 1    | 'abc'
`("for Bar, with s=$s n=$n expected=$expected", ({ s, n,  expected }) => {
  it("n should be ordinal: expected", () => {

    // test
    let result: string = new Bar().repeatStr(s, n);

    expect(result).toEqual('abcabcabc');
  });
  });
});
*/
